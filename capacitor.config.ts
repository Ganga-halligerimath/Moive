import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.moviedashboard',
  appName: 'MovieDashboard',
  webDir: 'build'
};

export default config;
